# Variáveis sensíveis
.env

# Dados do servidor (grandes demais — apenas conteúdo, não a pasta)
addons/
plugins/*
!plugins/.gitkeep

# Pastas de instalação local (geradas pelo install.sh / install.ps1)
cs2server/
steamcmd/

# Logs
*.log
bd.txt